runtime: nodejs20
instance_class: F1
env_variables:
  NODE_ENV: "production"
  # Buraya varsa diğer API anahtarlarınızı ekleyebilirsiniz
  # GEMINI_API_KEY: "buraya_anahtar"

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 0
  max_instances: 1
